[project]
name = "chatbi-engine"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "FlagEmbedding>=1.3,<2",
    "langchain-core>=0.3,<1",
    "psycopg[binary]>=3.2,<4",
    "qdrant-client>=1.14,<2",
]
