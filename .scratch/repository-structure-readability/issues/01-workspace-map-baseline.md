# 01: 建立全工作区事实地图与整理基线

**What to build:**

盘点整个工作区，建立与真实文件一致的结构基线，明确源码、测试、脚本、文档、数据库脚本、数据、模型、报告、本地环境和 IDE 配置的职责、维护方式和保留边界。

同时确认当前版本的真实来源，整理模块与文档导航关系，并形成大文件职责拆分候选和非缓存删除候选。本 Ticket 只建立事实和导航基线，不修改业务代码，不删除非缓存文件。

**Blocked by:**

None (can start immediately)

**Status:** done

## Acceptance criteria

- [x] 所有主要工作区目录都有明确分类、用途和保留理由。
- [x] 目录地图与实际文件和模块职责一致。
- [x] 当前版本来源被确认，或明确记录为待核对事实。
- [x] 大文件拆分候选按真实职责边界列出，并标注受影响的公共入口和验证方式。
- [x] 非缓存删除候选单独列出，没有提前删除。
- [x] 现有文档职责和入口关系被记录，不进行无目标内容瘦身。

## Result

已完成。基线记录见同目录的 `workspace-map.md`，内容包括：

- 顶层目录、源码模块、测试入口、文档职责和本地运行资产边界；
- 当前版本来源：`pyproject.toml` 为 `0.1.0`，HEAD 为 `v0.1.0-87-g5525e37` 开发快照；
- `README.md` 缺失、当前文档中的整体 `POC` 表述待由 Ticket 02 统一；
- `retrieval.py`、`sql_guard.py`、`service.py`、`tracing.py` 和 `export_schema.py` 的后续可读性拆分候选、公共入口和验证方式；
- 非缓存疑似候选的保留理由和删除影响。

本 Ticket 未修改业务代码、数据库、运行资产或非缓存文件，也未执行删除。

## Comments

本 Ticket 是后续 README、文档导航和内部代码整理的事实基线。不得把目录大小或文件数量本身直接当作拆分或删除依据。Ticket 02～07 可在本基线之上继续，Ticket 08 负责最终一致性验收。
