# ChatBI Domain Context

本文档只记录本次已确认的领域术语、业务含义和边界，不替代 Feature Spec、Module Spec 或实现设计。

## 多轮查询 Glossary

### Conversation（对话）

用户围绕同一个分析目标连续提出问题，并在后续问题中引用前面查询条件的业务上下文。一次对话不等于长期聊天记录。

### Active Conversation（当前会话）

用户当前可以继续追问的一段受控对话。V1 只保证当前会话内的连续追问，不保证页面关闭、会话过期或服务重启后的历史恢复。

### Turn（轮次）

用户提出一次问题并得到一次查询结果或受控失败的完整交互。只有成功的查询轮次才能成为后续追问的基础；失败轮次不改变之前的成功状态。合法但为空的查询结果仍属于成功轮次。

### Structured Query State（结构化查询状态）

描述最后一次成功查询的业务条件，包括指标、时间范围、维度和过滤条件。它表示用户当前正在分析什么，不等同于原始对话、SQL 文本或查询结果数据。

### Multi-Turn Query（多轮查询）

在同一当前会话中，沿用上一轮的结构化查询状态，新增或替换一个查询条件后重新执行一次受控查询。它不包含跨查询比较、趋势判断、原因归因或自动任务拆解。

## 已确认的业务边界

- 同一语义槽位的新值替换旧值；不同语义槽位的新条件可以叠加。
- 无法唯一理解的追问必须请求澄清，不能由模型猜测。
- 超出单条查询修订范围的问题不进入经营分析流程。
- 当前会话只属于当前认证用户，其他用户不能继续或读取它。
- 授权由当前认证身份和当前授权策略决定，历史会话状态不能赋予新的权限。
- 第一版面向内部或受控用户使用，不代表已经完成面向所有企业用户的最终 Production Ready（生产可用）能力。
